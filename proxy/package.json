{
  "name": "proxy",
  "private": true,
  "version": "1.0.0",
  "description": "proxy for Lets Hang",
  "main": "proxy.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Christian Grabowski",
  "license": "ISC",
  "dependencies": {
    "http-proxy": "^1.6.1"
  }
}
